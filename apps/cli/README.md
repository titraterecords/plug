# @plug/cli

The `plug` command-line tool. Discovers, installs, updates, and removes free VST3, AU, and CLAP audio plugins on macOS.

## Development

```bash
pnpm dev -- search reverb
pnpm test
pnpm build
```

## Commands

See `plug --help` and `plug <command> --help`.
