# tap

Plugin registry and Homebrew formulas.

`registry.json` is the single source of truth for all plugins. The CLI fetches it directly from this directory on GitHub.

## Adding a plugin

Add an entry to `registry.json`. See existing entries for the format. Schema defined in `packages/registry-schema`.
